spring:
  application:
    name: config-server
  cloud:
    config:
      server:8888
        git:
#          uri: file://${HOME}/config-repo
          uri: https://github.com/Djkmr/POC-Config-server.git
          searchPaths: server-config
