spring:
  application:
    name: config-server
  cloud:
    config:
      server:8888
        git:
#          uri: file://${HOME}/config-repo
          uri: https://github.com/Djkmr/config-server-POC-properties.git
          searchPaths: server-config
